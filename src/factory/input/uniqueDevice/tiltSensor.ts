import { inputPort } from '../inputPort'
import { SerialPort } from 'serialport'

// export const attachTiltSensor = (port: SerialPort, pin: number) => {
//   const tiltSensor = inputPort(port)(pin)

//   return {
//     on: async () => {
//       await tiltSensor.read()
//     },
//   }
// }
